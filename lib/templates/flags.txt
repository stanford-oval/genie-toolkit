thingtalk/en:
  all:
    always_filter, aggregation, bookkeeping, configure_actions, extended_timers, no_contextual_bookkeeping, nofilter, nostream, notablejoin, policies, primonly, projection, projection_with_filter, range_filters, remote_commands, schema_org, screen_selection, timer, triple_commands, undefined_filter
  default:
    always_filter, aggregation, bookkeeping, configure_actions, policies, projection, projection_with_filter, remote_commands, schema_org, screen_selection, timer, undefined_filter
  developer:
    always_filter, aggregation, bookkeeping, configure_actions, policies, projection, projection_with_filter, remote_commands, schema_org, screen_selection, timer, undefined_filter
