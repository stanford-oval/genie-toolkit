/* -*- mode: css -*- */

@name "ThingTV™ at %s:%d";
@description "This is a ThingTV. It shows stuff from the Internets.";

@setting ip-address {
    type: string;
}
@setting port {
    type: number;
}

#sink {
    url: 'http://' + @ip-address + ':' + string(@port) + '/api/switch-to';
}