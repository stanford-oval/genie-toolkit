/** @type {import('@jest/types').Config.InitialOptions} */
const config = {
  verbose: true,
  testMatch: ['**/?(*.)+(jest).ts'],
  moduleFileExtensions: ['js', 'ts', 'tsx'],
};

module.exports = config;
